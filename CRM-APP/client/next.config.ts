/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove or comment out experimental options causing the error
  // experimental: {
  //   turbopack: true,
  //   serverComponents: true,
  // },
};

module.exports = nextConfig;
